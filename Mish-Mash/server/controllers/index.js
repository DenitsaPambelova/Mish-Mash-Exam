const home = require('./home-controller')
const users = require('./users-controller')
const channels = require('./channels-controller')

module.exports = {
  home: home,
  users: users,
  channels: channels
}
